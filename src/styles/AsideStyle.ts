import { StyleSheet } from 'react-native';

export default function AsideStyle () {


  return StyleSheet.create({
    aside: {
      height: 7000,
      // backgroundColor: theme.mainColor,
    },
    ul: {

    },
    li: {
      // color: theme.textColor,
    }
  });
};
